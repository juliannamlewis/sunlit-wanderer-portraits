---
import BaseLayout from '../layouts/BaseLayout.astro';
import CopyProse from '../components/CopyProse.astro';
import GallerySection from '../components/GallerySection.astro';
import PricingSection from '../components/PricingSection.astro';
import ReviewsSection from '../components/ReviewsSection.astro';
import FaqSection from '../components/FaqSection.astro';
import PinterestBoards from '../components/PinterestBoards.astro';
import { loadCopy } from '../lib/markdown';
import gallery from '../content/galleries/grads.json';
import faq from '../content/faq/grads.json';

const { frontmatter, html } = loadCopy('grads');
---

<BaseLayout title={frontmatter.title} description={frontmatter.metaDescription}>
  <section class="section page-intro">
    <div class="container">
      <CopyProse html={html} />
    </div>
  </section>

  <GallerySection
    id="grads-gallery"
    folder="grads"
    images={gallery.images}
    eyebrow="Gallery"
    title="Grads"
  />

  <div class="section-alt">
    <PricingSection category="grads" />
  </div>

  <ReviewsSection placement="grads" />

  <FaqSection title={faq.title} questions={faq.questions} />

  <PinterestBoards category="grads" />
</BaseLayout>
