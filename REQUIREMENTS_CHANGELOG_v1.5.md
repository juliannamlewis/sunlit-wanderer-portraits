# Requirements Changelog — v1.4 → v1.5

The Sunlit Wanderer Portrait Site. Append to your existing changelog.

Last updated: September 4, 2026

---

## §53 — Railroad yard images: decided

**RESOLVED — use exactly one image, in the Seniors gallery only.** Confirmed
by Julie. Not listed as a location (unchanged from v1.4) — the property has
a **new owner who does not want photographers using the site, citing
liability risk**. This is a stronger reason than "posted no-trespassing" and
should be treated as permanent, not revisited casually.

Using a past portrait taken there is fine as a **portfolio image** — the
concern is about photographers continuing to use the physical location, not
about displaying previously taken photographs. Recommendation from
`PENDING_IMAGES.md` (one image, not three, so the gallery doesn't read as a
location on offer) stands and is now the confirmed approach.

**Still needed:** the actual clean, full-resolution image file. None of the
three candidate railroad frames (`IMG_0350`, `IMG_0345`, `IMG_0344` — see
`PENDING_IMAGES.md` Priority 6) has been located and exported yet. Julie will
provide one, or ask Claude to help locate it from the external drives, when
available. Until then, no railroad frame appears in `galleries/seniors.json`.

**No further discussion needed** on this item — it's closed pending only the
file itself.
